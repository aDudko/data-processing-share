input {
  file {
    path => "/logs/*.log"
    codec => "json"
    type => "logback"
  }
}

output {
  if [type]=="logback" {
    elasticsearch {
      hosts => [ "http://elasticsearch:9200" ]
      index => "logback-source-%{+YYYY.MM.dd}"
    }
  }
}